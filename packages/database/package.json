{
  "name": "database",
  "version": "0.0.0",
  "prisma": {
    "seed": "tsx seed.ts"
  },
  "scripts": {
    "dev": "npx tsc --watch",
    "build": "npx tsc",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "checkCompile": "tsc --noEmit --skipLibCheck"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "^5.8.1"
  },
  "devDependencies": {
    "prisma": "^5.8.1",
    "tsx": "^3.12.10"
  }
}
